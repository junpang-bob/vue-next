pipeline {
  agent any
  stages {
    stage('') {
      steps {
        timeout(time: 4, activity: true, unit: 'MINUTES') {
          input(message: 'ret', id: 'ert', ok: 'ert', submitter: 'ert', submitterParameter: 'ert')
        }

      }
    }

  }
}